---
- name: Apply AI Diagnostics deployment and service
  kubernetes.core.k8s:
    state: present
    kubeconfig: "{{ kubeconfig_path }}"
    definition: "{{ lookup('file', '../Kubernetes/ai-diagnostics.yml') | from_yaml_all | list }}"
