---
- name: Apply billing deployment and service
  kubernetes.core.k8s:
    state: present
    kubeconfig: "{{ kubeconfig_path }}"
    definition: "{{ lookup('file', '../Kubernetes/billing.yml') | from_yaml_all | list }}"
